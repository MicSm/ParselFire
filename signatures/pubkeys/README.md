# Public Keys

This directory stores ASCII-armored public verification keys for maintainers and
accepted external contributors.

Suggested naming:

- `maintainer-<short-fingerprint>.asc`
- `contributor-<short-fingerprint>.asc`

Only publish public keys here. Secret keys must never be committed.
