# Local Target Repositories

Clone repositories that you want to run ParselFire against into this directory.

This folder is intentionally gitignored so local target checkouts do not become
part of the ParselFire repository history.
